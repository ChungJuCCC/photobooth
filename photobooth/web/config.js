// Fill in after creating the Supabase project (see docs/SETUP.md).
// Both values are public by design: the publishable key has no access to
// any table, and get-session only answers for a valid session id.
window.GUEST_CONFIG = {
  functionsUrl: "https://YOUR-PROJECT-REF.supabase.co/functions/v1",
  publishableKey: "sb_publishable_REPLACE_ME",
  eventName: "",
};
